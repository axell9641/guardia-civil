export const environment = {
    production: true,
    firebaseConfig : {
    apiKey: {{API_KEY}} ,
    authDomain: {{AUTH_DOMAIN}},
    databaseURL: {{DATABASE_URL}},
    projectId: {{PROYECT_ID}},
    storageBucket: {{STORAGE_BUCKET}},
    messagingSenderId: {{MESSAGING_BUCKET}},
    appId: {{APP_ID}}
  }    
  };
  